{
  "name": "pg-tsquery",
  "version": "8.0.5",
  "description": "Parse user input into valid text search queries",
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "test": "eslint . && tsc --target es2018 --noEmit --alwaysStrict --allowJs --checkJs index.js && nyc node test",
    "pg": "docker run --rm -it -p 5432:5432 postgres:11"
  },
  "repository": "github:caub/pg-tsquery",
  "keywords": [
    "pg",
    "postgres",
    "PostgreSQL",
    "tsquery",
    "text",
    "search",
    "parser"
  ],
  "author": "caub",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/caub/pg-tsquery/issues"
  },
  "homepage": "https://github.com/caub/pg-tsquery#readme",
  "devDependencies": {
    "@types/node": "^12.0.12",
    "codecov": "^3.5.0",
    "eslint": "^6.0.1",
    "nyc": "^14.1.1",
    "pg": "^7.11.0",
    "typescript": "^3.5.2"
  }
}
